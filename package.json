{
  "name": "workspace",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "node scripts/build.js",
    "build:audit": "node scripts/build.js --audit",
    "audit": "node scripts/audit.js",
    "audit:save": "node scripts/audit.js --save",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "clean-css": "^5.3.3",
    "compression": "^1.8.1",
    "express": "^5.2.1",
    "express-rate-limit": "^8.5.2",
    "morgan": "^1.10.1",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "purgecss": "^8.0.0"
  }
}
