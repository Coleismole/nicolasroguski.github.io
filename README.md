# Nicolas Roguski — Personal Portfolio

Personal portfolio site for Nicolas Roguski. Plain HTML/CSS/JS served by a Node.js + Express backend. No frontend framework.

---

## Project Structure

```
/
├── server.js           # Express server — all backend logic lives here
├── package.json
├── scripts/
│   ├── build.js        # Asset pipeline: image conversion, CSS minification, version bumping
│   └── audit.js        # Lighthouse performance audit runner
└── public/             # Everything served to the browser
    ├── index.html      # Main single-page portfolio
    ├── myostatin-inhibitors.html  # Research deep-dive (subpage template)
    ├── 404.html
    ├── offline.html
    ├── manifest.json   # PWA manifest
    ├── robots.txt
    ├── sw.js           # Service worker (caching + offline)
    ├── assets/
    │   ├── css/
    │   │   ├── styles.css          # Edit this — main stylesheet source
    │   │   ├── styles.min.css      # Generated by `npm run build`
    │   │   ├── subpage-styles.css  # Edit this — research subpage stylesheet source
    │   │   ├── subpage-styles.min.css
    │   │   ├── critical.css        # Above-the-fold CSS (inlined into index.html)
    │   │   └── critical.min.css
    │   ├── fonts/
    │   │   └── fonts.css           # @font-face declarations + self-hosted .woff2 files
    │   ├── img/
    │   │   ├── nicolas-photo.jpg   # Source photo — run `npm run build` to regenerate WebP/AVIF
    │   │   ├── nicolas-photo.webp  # Generated
    │   │   ├── nicolas-photo.avif  # Generated
    │   │   └── favicon.svg
    │   ├── js/
    │   │   ├── main.js             # All homepage JS (GSAP animations, dark mode, etc.)
    │   │   └── analytics.js        # Client-side analytics beacon
    │   └── pdf/
    │       └── nicolas-roguski-cv.pdf
    └── vendor/                     # Self-hosted JS libraries (no CDN dependency)
        ├── gsap.min.js
        ├── ScrollTrigger.min.js
        ├── ScrollToPlugin.min.js
        └── lenis.min.js
```

---

## Quick Start

```bash
npm install
npm start          # Runs on port 5000
```

Open `http://localhost:5000` in your browser.

---

## NPM Scripts

| Command | What it does |
|---|---|
| `npm start` | Start the Express server on port 5000 |
| `npm run build` | Optimize images, minify CSS, inline critical CSS, bump version strings |
| `npm run build:audit` | Same as above + runs a Lighthouse performance audit |

**Always run `npm run build` after editing any `.css` file or replacing the hero photo.** The HTML files reference `.min.css` and versioned assets — the source files are not served directly.

---

## Environment Variables

| Variable | Required | Description |
|---|---|---|
| `ADMIN_TOKEN` | Recommended | Protects `/admin` and `/api/analytics/stats`. If unset, admin routes are public. |
| `ALLOWED_ORIGIN` | Optional | Restricts API CORS to a specific origin (e.g. `https://yoursite.com`). |
| `NODE_ENV` | Optional | Set to `production` to suppress stack traces in error responses. |

Set these as Replit Secrets (or export them locally before running):

```bash
export ADMIN_TOKEN=your-secret-here
npm start
```

---

## Server Routes

### Public
| Method | Path | Description |
|---|---|---|
| `GET` | `/` | Main portfolio page |
| `GET` | `/myostatin-inhibitors.html` | Research subpage |
| `GET` | `/sitemap.xml` | Auto-generated XML sitemap |
| `GET` | `/robots.txt` | Crawler rules |
| `GET` | `/health` | Server health + memory/cache stats |
| `POST` | `/api/analytics` | Record a page view (rate-limited: 30/min) |

### Admin (protected by `ADMIN_TOKEN`)
| Method | Path | Description |
|---|---|---|
| `GET` | `/admin?token=SECRET` | Analytics dashboard |
| `GET` | `/api/analytics/stats?token=SECRET` | Stats as JSON (rate-limited: 20/min) |
| `GET` | `/api/analytics/export?token=SECRET` | Download all data as CSV |

---

## Common Tasks

### Edit styles
1. Edit `public/assets/css/styles.css` (homepage) or `subpage-styles.css` (research pages)
2. Run `npm run build`
3. Refresh — the HTML always loads `.min.css` files

### Replace the hero photo
1. Drop the new photo in as `public/assets/img/nicolas-photo.jpg`
2. Run `npm run build` to regenerate WebP + AVIF versions

### Add a new research page
1. Copy `public/myostatin-inhibitors.html` as a template
2. Update `<title>`, `<link rel="canonical">`, and page content
3. Add the new path to `pages` in the `/sitemap.xml` route in `server.js`
4. Add the new path to the `PRECACHE` array in `public/sw.js`
5. Add a project card to the `#projects` section in `public/index.html`

### Bump the CSS/SW cache version
1. Increment `CSS_VERSION` in `scripts/build.js`
2. Run `npm run build` — it updates all version strings automatically

---

## Analytics

- No cookies, no personal data — stores path, referrer, session ID, and timestamp only
- Written to `analytics.json` (gitignored, runtime-only)
- Dashboard at `/admin?token=YOUR_TOKEN`
- Auto-pruned at 10,000 entries

---

## Tech Stack

| Layer | Technology |
|---|---|
| Runtime | Node.js |
| Server | Express 5 |
| Animations | GSAP 3 + ScrollTrigger + Lenis (self-hosted under `vendor/`) |
| CSS | Vanilla CSS with custom properties |
| JS | Vanilla JS (no framework) |
| Fonts | Self-hosted woff2 (Cormorant Garamond, DM Mono, Jost) |
| Analytics | Privacy-first, JSON file storage |
| Image build | sharp |
| CSS build | clean-css + PurgeCSS |

---

## Deployment (Replit)

- **Run command:** `node server.js`
- **Port:** 5000
- **No build step needed at deploy time** — `.min.css`, `.webp`, and `.avif` are committed

After deploying:
1. Set `ADMIN_TOKEN` as a production secret in Replit Secrets
2. Update `<link rel="canonical">` and `og:url` in HTML files to the production domain
3. Update the `Sitemap:` line in `public/robots.txt` to the production URL
